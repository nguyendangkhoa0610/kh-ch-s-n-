[deploy]
# startCommand đơn giản, đã chạy ổn định. db push chạy riêng qua `railway run`.
startCommand = "node api/dist/index.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 90
restartPolicyType = "on_failure"
