import type { Metadata } from "next";
export const metadata: Metadata = { title: "Tài khoản — Trầm Hương Eco-Resort" };
export default function L({ children }: { children: React.ReactNode }) { return <>{children}</>; }
